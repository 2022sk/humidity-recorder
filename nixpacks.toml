[phases.setup]
nixPkgs = ["python312"]
aptPkgs = ["fonts-nanum", "fontconfig"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "fc-cache -fv"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
